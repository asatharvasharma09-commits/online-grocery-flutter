name: grocery_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  share_plus: ^10.0.0
  shared_preferences: ^2.3.2
  provider: ^6.1.2
  image_picker: ^1.1.2
  fl_chart: ^0.68.0
  google_maps_flutter: ^2.7.0     # ✅ Keep only one entry
  geolocator: ^10.0.1
  geocoding: ^3.0.0
  permission_handler: ^11.0.1
  animations: ^2.1.0
  intl: ^0.20.2
  location: ^5.0.3               # ✅ Added for live GPS tracking

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/